// const element = document.createElement("span"); //By using createElement we can create new tag in html,
// element.innerText = "Hello World";    //By using innerText we can give an element to the new tag
// document.body.appendChild(element);   // By using appendChild we give a place for new tag(Child) inside of the body(parent) tag

// const id = document.getElementById("div-id");
// id.style.color = "red"; //Using id

// const Class = document.getElementsByClassName("div-class");

// const classArray = Array.from(Class);
// classArray.forEach(div => (div.style.color = "green")); //This is one way to achive colors for all elements

// for (let i = 0; i < Class.length; i++) Class[i].style.color = "green"; // This is another way to achive style color for all elements

// const body = document.createElement("body");
// const div = document.createElement("div");
// const span = document.createElement("span");
// const btn = document.createElement("button");
// const btn1 = document.createElement("button");
// btn.innerText = "change";
// btn1.innerText = "reset";
// span.setAttribute("id", "name");
// btn.setAttribute("id", "change");
// btn1.setAttribute("id", "reset");
// span.innerText = "Sitaramaraju";
// document.body.appendChild(div).appendChild(span);
// document.body.appendChild(div).appendChild(btn);
// document.body.appendChild(div).appendChild(btn1);

// const uname = document.getElementById("name");
// const btnChange = document.getElementById("change");
// const btnReset = document.getElementById("reset");
// btnChange.style.cursor = "pointer";
// btnReset.style.cursor = "pointer";
// btnChange.onclick = () => {
//   uname.innerText = "Hello";
// };
// btnReset.onclick = () => {
//   uname.innerText = "Sitaramaraju";
// };
